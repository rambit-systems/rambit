[package]
name = "site-paddle"
version = "0.1.0"

edition = "2024"
license-file.workspace = true
publish = false

[dependencies]
# models = { path = "../models", default-features = false }

# serde_json.workspace = true
